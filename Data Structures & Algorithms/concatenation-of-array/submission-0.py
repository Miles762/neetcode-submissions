class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        add = []
        for i in range(2):
            for num in nums:
                add.append(num)
        return add
       
            
        